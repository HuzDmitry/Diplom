server:
  port:
    88
spring:
  application:
    name=TestFireAlarm
  security:
    debug:
      false
  datasource:
    url: jdbc:mysql://localhost:3306/alarm
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true
